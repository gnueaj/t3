Mode: built-in image_gen edit, followed by color-only compositing onto the original glyphs.
Source: teaser-final-before-purple-title.png
Final asset: teaser-final.png
Weave title color sampled from generated edit: #8D61FB.
Adjacent title spool icon saturation: original × 1.10; original hue, brightness and geometry retained.
All pixels outside the two title regions verified identical to the source.

Use case: precise-object-edit / local recoloring.
Input image 1 is the EDIT TARGET, the current finished Weave teaser, 1672 x 941 pixels.
Make exactly two COLOR-ONLY changes in the large upper-right headline "그때 그거? Weave [purple spool icon] got it!":
1. Change only the large word "Weave" from white to a clean purple matching the adjacent spool-of-thread icon. Keep precisely the same original font, glyph contours, weight, size, kerning, baseline and position. Use a consistent purple fill, approximately #9565F7, harmonious with the thread icon.
2. Slightly increase only the purple saturation of the immediately adjacent large spool-of-thread icon, about 10% relative saturation increase. Keep its hue family and brightness nearly identical, preserving every line, curve, shading detail, highlight and edge. A small vividness increase, not a dramatic hue or brightness change.
Absolute preservation: keep "그때 그거?" and "got it!" solid white and exactly unchanged. Keep the subtitle, the phone UI's small "Weave" title and its small spool icon, the bottom-left "Weave Assist" and tagline, all purple connecting threads, every other icon, every chat message, person, hand, photo, cake, map, background pixel, logo, lighting and shadow unchanged. Do not redraw, resize, reshape, reposition, sharpen, crop or regenerate any other area. Only recolor the large headline word Weave and slightly saturate its adjacent large purple spool. Return the full image at exactly the input dimensions.
