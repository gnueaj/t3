# 변환·검수 기록 · 2026-09-22

## 범위

`index.html` 기준본 11장과 `index-experimental.html` 실험본 11장을 각각 검수했습니다. 두 버전은 표지 외 2~11쪽이 동일합니다. PDF와 PPTX는 각 버전의 동일한 HTML 내용·배치를 기준으로 생성했습니다.

## 글꼴과 배치

- 한국어 Noto Sans KR, 영어·숫자 Inter. 400·500·600·700의 총 8개 폰트 파일을 PPTX에 내장했습니다.
- 중간 굵기는 `Inter Medium`, `Inter SemiBold`, `Noto Sans KR Medium`, `Noto Sans KR SemiBold`라는 내부 패밀리 이름으로 매핑했습니다.
- 텍스트를 브라우저에서 측정한 줄·폰트 구간별로 배치했습니다. 음수 자간을 유지하며 자동 줄바꿈과 자동 축소를 사용하지 않습니다.
- 그림·배경·UI 도형은 HTML 렌더링의 비텍스트 층을 2560×1440 PNG로 고정했습니다. 텍스트는 이 위의 별도 편집 가능한 PowerPoint 개체입니다. 표의 문구도 편집 가능하지만 선·배경의 재배치는 HTML에서 하는 편이 적합합니다.
- 출처는 도형 수준 하이퍼링크로 연결해, PowerPoint 기본 링크 색상이 원래 글자색을 덮어쓰지 않도록 했습니다.

## 확인 결과

| 검사 | 기준본 | 실험본 |
| --- | --- | --- |
| HTML 슬라이드 / PDF 페이지 | 11 / 11 | 11 / 11 |
| 외부 네트워크 차단 상태 HTML 실행 | 통과 | 통과 |
| HTML 이미지 누락·텍스트 경계 초과 | 0 / 0 | 0 / 0 |
| PPTX 텍스트 내용 누락 | 0 | 0 |
| 재렌더링 시 추가 줄바꿈 | 0 | 0 |
| 재렌더링 시 오른쪽 경계 초과 | 0 | 0 |
| HTML 대비 텍스트 기준선 최대 편차 | 0.072px | 0.072px |

텍스트 내용은 PPTX XML과 PDF 추출 결과로 대조했고, 주요 수정 페이지와 두 표지를 이미지로 확인했습니다. 사진 안에 원래 포함된 UI 글자는 래스터 이미지이므로 별도 텍스트 비교 대상이 아닙니다. 최신 티저의 프로필·아이콘·문구 수정 영역 외 픽셀 동일성은 `assets/teaser-refinement-validation.json`에 기록했습니다. 최초 아이콘 세 곳 교체 검증은 `assets/teaser-icon-validation.json`에 이력으로 남겼습니다.

최신 재출력에는 새 프로필·축소 UI 실타래·책 문구가 보이지 않는 표지 크롭을 반영했습니다. 첫 장 상단 영문 설명과 11장 모두의 반복 영문 푸터는 원본 HTML 및 PPTX 텍스트에서 삭제했고, 페이지 번호는 오른쪽에 유지했습니다. 티저 제목·하단 홍보 문구의 실제 폰트 로드도 별도로 확인했습니다.

## 검수 한계와 편집 안내

PPTX 검수에는 LibreOffice 24.2를 사용했고, 동일 폰트를 별도로 제공한 환경에서 렌더링했습니다. 이 버전의 LibreOffice는 PPTX 내장 폰트를 직접 활용하지 않으므로, 내장 여부는 패키지 구조로 별도 확인했습니다. **실제 Microsoft PowerPoint에서 직접 실행한 검수는 아닙니다.** 모든 모바일 뷰어에서 폰트 내장을 동일하게 지원한다고 보장할 수 없습니다.

단어를 긴 문장으로 바꾸면 텍스트 상자 위치·폭도 조정해야 할 수 있습니다. 여러 글꼴 구간은 한 줄 그룹으로 묶여 있습니다. 큰 문구·레이아웃 수정은 원본 HTML에서 진행하고 다시 내보내는 것이 가장 일관됩니다. 단순 열람·발표에는 HTML 기준 PDF가 가장 안정적입니다.

## 보존 확인

기획서 MD의 Page 5~7 및 Page 10은 수정 전과 동일함을 대조했습니다. Page 11 PPT에서는 평가 기준 표를 제거하고 위에 서술된 세 기대효과를 반영했습니다. 기존 문서와 발표자료는 `0920/before-0922/`에 백업했습니다.
